let width = 500;
let height = 400;
let circles = [];
let rains = [];

function setup() {
  width = windowWidth;
  height = windowHeight;
  createCanvas(width, height);
  // let c, rx ,ry, s;
  // for (let i = 0; i < 500; i++) {
  //   rx = random(0, width);
  //   ry = random(0, height);
  //   s = random(15, 30);
  //
  //   c = new Circle(rx, ry, 0 ,0, 0, 0.05, s);
  //   circles.push(c);
  // }
  // print(circles);

  // let rain, rx, ry, vy;
  // for (let i = 0; i < 1000; i++) {
  //   ry = random(0, 500);
  //   rx = random(0, width);
  //   // vx = random(0, 1);
  //   vy = random(4, 7);
  //   rain = new Rain(rx, -ry, 0, vy, [138, 43, 226, 100]);
  //   rains.push(rain);
  //
  // }
}

function draw() {
  background(0);
  // for (let circle of circles) {
  //   circle.display();
  //   circle.move();
  // }

  // for (let rain of rains) {
  //   rain.display();
  //   rain.move();
  //
  // }


}
